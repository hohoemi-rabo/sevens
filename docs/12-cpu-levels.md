# 12. CPU補完（弱・中・強）

**フェーズ**: 3　**依存**: 06　**対象**: `src/lib/sevens/cpu/{weak,medium,strong}.ts`

## 概要
CPUの思考を弱・中・強の3段階に作り分け、ホスト設定（11）で選択できるようにする。参加者が4人未満のとき自動でCPUを追加する。

## 関連要件
- `REQUIREMENTS.md` 3.4（CPUプレイヤー）

## 仕様メモ
- 06で定義した思考インターフェース `decide(state, playerId): Action` を3実装で差し替える
- **弱**：出せる札があればほぼ即出し。戦略性なし（生徒さんが勝ちやすい）
- **中**：自分の手札の連続性を見て、止めると有利な札は少し溜める
- **強**：相手の動きを読み、キーカード（端に近い札）を戦略的に止める
- 思考時間 1〜2秒の演出（待たせすぎない）
- 各CPUに名前・アバター付与（例「りつこ」「ハジメ」「ミミ」）。アバター素材の置き場所も決める
- CPUは人間と同じパス・脱落ルール（07）に従う

## Todo
- [x] `weak.ts`：出せる札を即出し／なければパス（チケット06）
- [x] `medium.ts`：自分の手札の連続性＋盤面で、外側を出し7寄りのゲート札を温存（自分中心・相手は覗かない）
- [x] `strong.ts`：相手の実手札を読み、得させる札を避け、上がり間近の相手のキーカードをパスで止める
- [x] ホスト設定（11）から強さを選択しCPUに反映（`strategyFor`＝弱/中/強。配線は#11で完了）
- [x] 人数不足時に自動でCPUを4人まで補完（`fillWithCpu`・チケット10/11）
- [~] CPUの名前は付与済み（りつこ/ハジメ/ミミ/サブ）。**アバター素材・表示はUI仕上げ（#16/#17）へ送る**
- [x] 思考時間 1〜2秒の演出（`server.ts` の `driveAutoTimed`/`cpuDelay`・チケット10）

## 完了条件
- [x] 弱/中/強で明確に挙動差があり、設定どおりに動く
  - 決定論的な挙動テスト（中≠弱の選択差、強の相手回避・キーカード温存）＋全員同レベル/混在での完走テストで担保（`cpu/{medium,strong,heuristics}.test.ts`）
- [x] 4人未満でも自動補完で対局が成立する（`fillWithCpu`＋`advanceAuto`/`driveAutoTimed`）

## 設計メモ（確定）
- 共通ヘルパ `cpu/heuristics.ts`：`centrality(rank)`（7が最大＝ゲート性）／`opponentGain`（出すと相手の出せる札が何枚増えるか・強が相手手札を参照）／`threatHandSize`（最も上がりに近い相手の手札枚数）。
- 中＝自分中心（相手は覗かない・パスしない）、強＝相手読み＋戦略的温存（パス）。乱数なし＋`cardId` タイブレークで決定論的。パス回数は有限なので温存しても必ず終局する。
- アバターは**置き場所も未確定のまま#16/#17へ送る**（思考ロジックに集中というスコープ判断）。
