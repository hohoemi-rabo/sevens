# 18. 将来拡張（ジョーカー・ローカルルール・クラウド）

**フェーズ**: 5（スコープ外・必要時に着手）　**依存**: コア完成後

## 概要
教室で必要になったら着手する将来拡張。現時点ではスコープ外（`REQUIREMENTS.md` 9）。着手時にこのチケットを具体化・分割する。

## 関連要件
- `REQUIREMENTS.md` 8 フェーズ5、9（スコープ外）

## 仕様メモ
- **ジョーカー追加**：任意位置への配置・差し替え処理。標準ルールでは使わないため、必要時のみ
- その他ローカルルール：複数枚出し、スペードのKでストップ等
- クラウド対戦：`RemoteAdapter` を追加（10の抽象を流用）。Railway / Fly.io 等
- 配布：Tauri / Electron でワンクリック起動アプリ化（`REQUIREMENTS.md` 6.6）

## Todo（着手時に具体化）
- [ ] ジョーカーの仕様確定（配置・差し替え・UI）
- [ ] ローカルルールの選定と実装
- [ ] `RemoteAdapter`（クラウド対戦）の設計
- [ ] Tauri / Electron でのアプリ化

## 完了条件
- 着手時点で本チケットを個別チケットへ分割し、要件を確定してから実装する
