# 09. ゲームロジック単体テスト

**フェーズ**: 2　**依存**: 01〜03, 07, 08　**対象**: `src/lib/sevens/**`（テスト）

## 概要
ゲームロジック層（純粋TS）の単体テストを整備する。テストフレームワークは未導入のため、まず選定・導入から行う。振る舞い（出力）をテストし、実装詳細に依存しないこと。

## 関連要件
- `REQUIREMENTS.md` 開発ロードマップ フェーズ2（ルールの単体テスト）、`CLAUDE.md`「テスト規律」

## 仕様メモ
- 候補：Vitest（Next.js/TS と相性が良く高速）。導入したら `package.json` に `test` スクリプトを追加し、`CLAUDE.md` のコマンド欄を更新する
- 純粋関数中心なので、乱数（シャッフル）はシード固定で再現性を確保
- エラーケース（不正な配置・不正なパス）も必ずカバー

## Todo
- [x] テストランナー選定・導入（Vitest 想定）
- [x] `package.json` に `test` スクリプト追加、`CLAUDE.md` のコマンド欄を更新
- [x] 配札（01）のテスト：52枚・重複なし・各13枚
- [x] 出せる札判定（02）のテスト：7起点・両方向・端（A/K）
- [x] 状態遷移（03）のテスト：出す/パス/手番送り/終了判定
- [x] パス・脱落（07）のテスト：上限超過で脱落・手番除外
- [x] 順位判定（08）のテスト：上がり順・脱落の表示
- [x] CPU（06）の最低限の挙動テスト

## 実装メモ
- テスト（Vitest）は **01〜08 の各チケットで並走導入済み**。09では網羅性を点検し不足分を補完した。
- カバレッジ計測を導入: `@vitest/coverage-v8` ＋ `npm run test:coverage`（`vitest.config.ts` の `coverage`、対象 `src/lib/sevens/**`、生成物 `coverage/` は gitignore＋eslint ignore）。
- 補完した異常系: 終了後の `playCard`/`pass` 例外、`initGame` 不正 maxPass、`deal` 不正人数、`pass` 手番違い、`isWastefulPass` 不明ID。
- 統合テスト `rules.integration.test.ts`: 1ゲームを台本通りに進め、脱落含む最終順位（`computeStandings`）まで end-to-end 検証。
- カバレッジ: ロジック層 約99% stmts / 96% branch / 100% funcs。残りはソート比較子内の `?? 0` 防御分岐と到達不能な防御コードのみ（不変条件違反でしか踏めず、指標より質を優先して許容）。

## 完了条件
- `npm test`（または相当）でロジック層のテストがグリーン
- 正常系・異常系の両方がカバーされている
